CREATE TABLE CLAIM (
	CLAIMID NUMBER(10) PRIMARY KEY,
	POLICYID NUMBER(10),
	ACCIDENTID NUMBER(10),
	STATUS VARCHAR2(20) NOT NULL,
	CREATEDON TIMESTAMP,
	UPDATEDON TIMESTAMP,
	ASSIGNEDTO VARCHAR2(50),
	REJECTEDREASON VARCHAR2(255)
);


ALTER TABLE CLAIM
ADD FOREIGN KEY (POLCIYID) 
REFERENCES POLICY(POLICYID);